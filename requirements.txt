Flask==3.0.3
SQLAlchemy==2.0.36
alembic==1.14.0
PyMySQL==1.1.1
cryptography==44.0.0
python-dotenv==1.0.1
APScheduler==3.10.4
playwright==1.49.0
tf-playwright-stealth==1.2.0
beautifulsoup4==4.12.3
lxml==5.3.0
requests==2.32.3
browser-cookie3==0.20.1
# Optional: rookiepy handles Chrome 127+ app-bound encryption without admin,
# but as of 0.5.6 has no prebuilt wheel for Python 3.13 (source build requires
# an older pyo3). If you're on Python <=3.12, `pip install rookiepy` enables it
# and refresh_funda_cookies.py will pick it up automatically.
